# ---- .NET ----
bin/
obj/
publish/
*.user
*.suo
.vs/
*.zip

# ---- Terraform ----
.terraform/
# .terraform.lock.hcl is intentionally NOT ignored — commit it (after `terraform init`)
# so collaborators/CI resolve identical, pinned provider versions.
*.tfstate
*.tfstate.*
# Saved plan files can contain sensitive values — never commit them.
*.tfplan
tfplan.binary
crash.log
crash.*.log
terraform.tfvars
*.tfvars.json
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# ---- Secrets / local ----
*.pfx
*.pem
appsettings.Local.json
.env
